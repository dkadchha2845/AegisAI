# Dataset generation (offline, one-time, free backends)
httpx>=0.27

# Only needed if AEGIS_LLM=anthropic (paid). Not required for gemini/ollama.
# anthropic>=0.69

# Training runs in Colab (free T4), not locally:
# transformers>=4.44  torch>=2.3  scikit-learn>=1.5  datasets>=2.20
